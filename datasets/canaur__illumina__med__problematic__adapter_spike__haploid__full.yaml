# canaur_sim_adapter_001: adapter-only spike (reads carrying adapter at both ends)
# Schema: schema/dataset.schema.json
schema_version: "1.0"
dataset_id: "canaur__illumina__med__problematic__adapter_spike__haploid__full"

genome: "canaur"

technology: "illumina"
coverage: "medium"
coverage_targets: { coverage_x: 30 }

quality: "problematic"
degradation:
  - mechanism: adapter_only
    params: { fraction: 0.01 }
    # adapter-only reads (both-end adapter dimers) spiked at a low fraction (ADR-014)

contamination: {}

ploidy:
  expected: haploid
  actual: haploid

subset:
  mode: "full"

source_reads:
  type: "simulated"
  accessions: []
  generator: "0.1.0"
  seed: 20260807

notes: "ADR-015 canaur_sim_adapter_001 (ADR-014 adapter-only spike): a fraction of reads carry adapter at both ends; models adapter-dimers in the library. Separate unit from carryover."
